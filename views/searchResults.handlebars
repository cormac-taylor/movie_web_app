    <a class="to_home" href="/">
        Search for Another Movie
    </a>
{{#if noErrors}}
    <h1>{{pageTitle}}</h1>
    <h2 id="searchByTitle">{{searchByTitle}}</h2>
    <main>
    <ol>
        {{#each movies}}
        <li>
            <a href="/getmovie/{{imdbID}}">
            {{Title}}
            <br>
            {{!-- make defualt no_image.jpeg --}}
            <img
                class="searchResultImage"
                alt="{{Title}} Poster"
                src={{Poster}}
            >
            </a>
        </li>
        {{/each}}
    </ol>
    </main>
    <a class="to_home" href="/">
        Search for Another Movie
    </a>
{{else}}
    <p class="movie-not-found">We're sorry, but no results were found for {{searchByTitle}}.</p>
{{/if}}

